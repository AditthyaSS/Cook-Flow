name: cookflow_app
description: CookFlow - Beautiful recipe extraction mobile app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  sqflite: ^2.3.0
  path: ^1.8.3
  share_plus: ^7.2.1
  url_launcher: ^6.2.1
  cupertino_icons: ^1.0.2
  intl: ^0.18.1
  shared_preferences: ^2.2.2
  shimmer: ^3.0.0
  # Firebase dependencies (Phase 3)
  firebase_core: ^2.24.0
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  google_sign_in: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
